/**
 * 业务模块根包（占位）。各子模块按 TECH-P1 §1 拆分：
 * mind(意识档案) / space(意识空间) / resonance(共鸣匹配) / echo(回声) /
 * social(社交) / account(账号) / avatar(形象)。
 *
 * <p>BE-1 仅建占位，实体/协议/服务逻辑在 BE-2~BE-5 落地。</p>
 */
package com.echo.module;
